version: '3.1'

services: 
    mysql:
        container_name: test-golang-mysql
        image: mysql:latest
        restart: always
        environment: 
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: test-golang
        ports: 
            - 3306:3306
        volumes:
            - ./.docker/data/mysql/:/var/lib/mysql
            - ./.docker/logs/mysql/:/var/log/mysql